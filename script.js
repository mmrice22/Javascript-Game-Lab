"use sctipt"
const randomDamage = () => console.log(Math.floor(Math.random() * 10) + 1);
// to show that function above works
// console.log(randomDamage());

const randNum = (opt1, opt2) => console.log(Math.round(Math.random()));
// to show that the function above works
// console.log(randNum());
